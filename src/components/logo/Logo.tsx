import styled from "styled-components";

export const Logo = () => {
    return (
        <StyledLogo href='#'>
            {'<devDiana/>'}
        </StyledLogo>
    );
}

const StyledLogo = styled.a`

`